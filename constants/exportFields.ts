export const PRODUCT_FIELDS = ["id", "title", "featured", "createdAt"];
export const INQUIRY_FIELDS = ["id", "name", "email", "status", "createdAt"];
export const AGENT_FIELDS = ["id", "name", "type", "enabled", "createdAt"];
export const LEAD_FIELDS = ["id", "name", "email", "status", "createdAt"]; // Assuming leads come from inquiries for now
